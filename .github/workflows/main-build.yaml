name: Build main

on:
  push:
    branches:
      - main
    paths-ignore:
      - integration-tests
      - files
  workflow_dispatch:

jobs:
  call-workflow-build-image:
    uses: ./.github/workflows/build-image.yaml
